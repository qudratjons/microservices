server:
  port: 0
  shutdown: graceful
spring:
  application:
    name: order
  datasource:
    url: jdbc:postgresql://localhost:5432/order
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka