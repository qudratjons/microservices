server:
  port: 0
  shutdown: graceful

spring:
  application:
    name: product
  data:
    mongodb:
      uri: mongodb://localhost:27017/product-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka